#  Backend 
PORT=3001
NODE_ENV=development

# URL of the ML prediction service
ML_SERVICE_URL=http://localhost:5000

# CORS allowed origin (use * for development)
CORS_ORIGIN=*

# ML Service 
# PORT=5000  (set in docker-compose)

#  Frontend (Vite) ────────────────────────────────────────────────────────
# VITE_API_URL=http://localhost:3001  (defaults to proxy in dev, not needed)
